export enum LeadSource {
  INSTAGRAM = "instagram",
  REFERRAL = "referral",
  WEBSITE = "website",
}

export enum LeadStatus {
  CONTACTED = "contacted",
  LOST = "lost",
  NEW = "new",
  QUALIFIED = "qualified",
}

export enum UserRole {
  ADMIN = "admin",
  SALES = "sales",
}
